## plop

## plop使用
```js
1. yarn add plop --dev (安装开发依赖)
2. 创建 plopfile.js (入口文件)
3. 根目录创建一个plop-template文件夹
4. plop-template下, 创建 .hbs模板文件
5. 执行命令 yarn plop component (component 为plopfile.js 中生成器的名字)
6. 根据 为plopfile.js 的prompts命令行交互, 输入生成的文件名字
```
